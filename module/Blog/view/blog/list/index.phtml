<?php

$title = 'Blog';
$this->headTitle($title);

?>

<h1>
    <?= $this->escapeHtml($title) ?>
</h1>

<p>
    <a href="<?= $this->url('blog', ['action' => 'create']) ?>">
        Add post
    </a>
</p>

<table class="table">
    <thead>
        <th>Title</th>
        <th>Description</th>
        <th>Action</th>
    </thead>
    <tbody>
    <?php foreach ($blogs as $blog) : ?>
    <tr>
        <td> <?= $this->escapeHtml($blog->title) ?> </td>
        <td> <?= $this->escapeHtml($blog->description) ?> </td>
        <td></td>
    </tr>
    <?php endforeach; ?>
    </tbody>

</table>